import { categorySchema } from "./schemas/category";
import { productSchema } from "./schemas/product";
import { articleSchema } from "./schemas/article";

export const schemaTypes = [categorySchema, productSchema, articleSchema];
